{
  "name": "webviews",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "pushd \"../ui\" && npm install && popd && pushd \"./editor\" && npm install && popd"
  },
  "author": "",
  "license": "ISC"
}
